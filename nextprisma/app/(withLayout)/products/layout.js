export const metadata = {
  title: 'Subscribe',
  description:
    'Become a member, support the movement, and get access to benefits!',
}
import React from 'react'

export default function layout({ children }) {
  return <>{children}</>
}
