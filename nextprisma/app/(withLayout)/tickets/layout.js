export const metadata = {
  title: 'Support Tickets',
  description: 'See all of the requests here',
}
import React from 'react'

export default function layout({ children }) {
  return <>{children}</>
}
