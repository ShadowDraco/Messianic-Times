export const metadata = {
  title: 'Sign up',
  description: 'Join the movement! Join the times!',
}
import React from 'react'

export default function layout({ children }) {
  return <>{children}</>
}
