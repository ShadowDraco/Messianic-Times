export const metadata = {
  title: 'Account',
  description: 'Manage settings and subscriptions',
}
import React from 'react'

export default function layout({ children }) {
  return <>{children}</>
}
