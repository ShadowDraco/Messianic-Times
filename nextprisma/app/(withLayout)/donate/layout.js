export const metadata = {
  title: 'Donate',
  description: 'Support the Jews through the Messianic Times',
}
import React from 'react'

export default function layout({ children }) {
  return <>{children}</>
}
